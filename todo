- column: option for default sort order
- columntype: add to interface a virtual get_option()
- config: add rename_option() ? (only doable through provider now)
- config: limit len of names? (columntype-name has 128 for
  "treeviews/$name/columns/$name/sort_$name")

- document "option paths" -- e.g. columntype options should be looked for in
"treeviews/$tv_name/columns/$col_name/$opt_name" and then if not found in
"columns/$col_name/$opt_name" (and if not found there may be some app-default
to be looked in "default/$opt_name" or something (might be subcat of defaults))

- tree has hashtable, keys are node pointers
- set a default log_handler to show g_warning-s, or use a donna_warning

- columntype: handle_context: add renderer, x ,y
- columntype & provider: add prepare_context_menu
- columntype: cb to get list of req/opt options

- node: setter should take care of return value, not node
- node: err msg "prop %s on node" should add "node %s:%s",domain,location

- a few recent places (config, etc) need to use GError
