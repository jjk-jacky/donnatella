
#ifndef __DONNA_TASK_MANAGER_H__
#define __DONNA_TASK_MANAGER_H__

#include "provider-task.h"

/* empty because we haven't made an interface DonnaTaskManager (that
 * provider-task would also implement), but it might happen some day. Until
 * then, all required API is on provider-task.h but when needing to use the
 * config, one should include this very task-manager.h */

#endif /* __DONNA_TASK_MANAGER_H__ */
